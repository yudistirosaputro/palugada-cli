# Rust build artifacts
/target

# macOS
.DS_Store

# Design doc — kept locally, not committed
PRD-unified-palugada.md

# npm packaging build output
npm/.staging/
npm/dist/
node_modules/

# superpowers brainstorm mockups
.superpowers/

# palugada self-dogfood: keep the profile binding (config.yaml) but not the
# generated symbol index (regenerable via `palugada index`) or personal IDE state.
.palugada/index/
.claude/settings.local.json
